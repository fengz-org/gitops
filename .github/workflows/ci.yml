---
    name: Checks

    on:
      push:
        branches: ['**']


    permissions:
      actions: read
      attestations: read
      checks: read
      contents: read
      deployments: read
      id-token: none
      issues: read
      discussions: read
      packages: read
      pages: read
      pull-requests: read
      repository-projects: read
      security-events: read
      statuses: read

    jobs:
      checkov:
        name: IaC security scan
        needs: []
        uses: 'https://github.com/fengz-org/gitops/blob/main/.github/workflows/checkov.yml'
        with:
          FAIL_ON_SECURITY_ISSUES: 'true'
          FRAMEWORK: 'terraform'
